[build]
builder = "python"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "gunicorn -w 4 -k uvicorn.workers.UvicornWorker app.main:app -b 0.0.0.0:8000"
healthCheckPath = "/"

[http]
port = 8000 